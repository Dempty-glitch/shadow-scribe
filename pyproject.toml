[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]  # line too long — prompts strings sẽ luôn dài

[tool.mypy]
python_version = "3.9"
ignore_missing_imports = true
